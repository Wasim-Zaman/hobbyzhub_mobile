class AppConfig {
  static int pageOne = 0;
  static int categoriesPageSize = 1000;
  static int categoryPageSize = 10;

// Adjust this value to control the position to trigger loading
  static double loadOnScrollHeight = 400.0;
}
